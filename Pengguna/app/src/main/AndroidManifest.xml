<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.pengguna">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".KonfPesanActivity"></activity>
        <activity android:name=".HelperActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps">

        </activity>>
        <activity android:name=".VerifActivity" />
        <activity android:name=".LihatProyekActivity" />
        <activity android:name=".OAWActivity" />
        <activity android:name=".LasUmumActivity" />
        <activity android:name=".ListProyekActivity" />
        <activity android:name=".SMFCAWActivity" />
        <activity android:name=".SMGTAWActivity" />
        <activity android:name=".GTAWActivity" />
        <activity android:name=".FCAWActivity" />
        <activity android:name=".SMAWActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".IntroActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ManufakturActivity" />
        <activity android:name=".PipaActivity" />
        <activity android:name=".KapalActivity" />
        <activity android:name=".ListSFCActivity" />
        <activity android:name=".KonstruksiActivity" />
        <activity android:name=".Steel" />
        <activity android:name=".JenisProyek" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>